[package]
name = "nsp-netlist"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
nsp-core = { path = "../nsp-core" }
nsp-schema = { path = "../nsp-schema" }
